export function createSupabaseAdapter() {
  throw new Error('Adapter Supabase ainda não implementado. A interface de persistência da v10 já está pronta para receber essa integração.');
}

export default createSupabaseAdapter;
